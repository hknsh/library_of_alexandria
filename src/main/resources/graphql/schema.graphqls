type Query {
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    summary: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}